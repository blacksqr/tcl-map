#XXX palette email:nap-alpha-patch&segfault&ReadRasterNAP&HDF/netcdf optional
#XXX toolbar zoom info lat/long
#XXX OGR tcltest, OGR GISViewer

# loadworldfile zoom-limits centered-zoom grab progressbar_when_opening
# XXX starpack enhanced-zoom tiled/centered-background
# wrap setprojects etc..
# Units value (e.g. elevation as described in GetDescription in units of GetUnitType and resolution of GetDatatype) = (raw pixel value * GetScale) + GetOffset

NAP 10.5.11 hsv2rgb(hsv)

NAP 5.5.11 and 10.4.5. GIS

GDALLoadWorldFile
GDALGetProjectionRef 
GDALSetProjectionRef
GDALSetGeoTransform
GDALGCPsToGeoTransform
set GCPs manually
GDALGetGCPProjection (GDALDatasetH)
GDALGetRasterCategoryNames
RAT
MaskBand